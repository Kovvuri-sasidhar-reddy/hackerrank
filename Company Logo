from collections import Counter

s = input().strip()

# Count frequency of each character
freq = Counter(s)

# Sort by frequency (descending) and alphabet (ascending)
result = sorted(freq.items(), key=lambda x: (-x[1], x[0]))

# Print top 3
for char, count in result[:3]:
    print(char, count)
